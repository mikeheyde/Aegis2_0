# 2026-04-23

- Corrigi o agente `xing-ling` após falha silenciosa. Causa raiz mais provável: `~/.openclaw/agents/xing-ling/agent/models.json` estava desatualizado (Codex apontando para `https://chatgpt.com/backend-api/v1`) e `auth-profiles.json` estava vazio.
- Fiz backup em `~/.openclaw/agents/xing-ling/agent/backup-20260423-162024/` e sincronizei `models.json` + `auth-profiles.json` a partir do agente `main`.
- Validação concluída com sucesso via `openclaw agent --agent xing-ling`: resposta `XING LING operacional`.
